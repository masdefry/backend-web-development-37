version: '3.9'

services:
  nextjs:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://localhost:5000/api
    container_name: pos-app
    ports:
      - '3000:3000'
    restart: always
